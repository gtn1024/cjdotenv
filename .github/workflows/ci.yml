name: ci

on: [push, pull_request]

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-latest-arm, macos-latest, windows-latest]
        cangjie-versions: [1.0.0, 1.0.1, 1.0.2, 1.0.3, 1.0.4, lts]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: Zxilly/setup-cangjie@v2.0.0
        with:
          version: ${{ matrix.cangjie-versions }}
      - name: build
        shell: bash
        run: cjpm build
      - name: test
        shell: bash
        run: cjpm test
